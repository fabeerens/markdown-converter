#!/usr/bin/env bash
# Dubbelklik dit bestand in Finder om de EUR-Lex → Markdown tool te starten.
# Het opent een Terminal-venster, installeert zo nodig de dependencies,
# start de server en opent je browser. Sluit het venster om te stoppen.
cd "$(dirname "$0")"
./run.sh
